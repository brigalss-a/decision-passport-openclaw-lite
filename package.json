{
  "name": "decision-passport-openclaw-lite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "pnpm run typecheck",
    "demo": "tsx scripts/run-demo.ts",
    "test": "vitest run",
    "checksums": "tsx scripts/generate-checksums.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.5.2",
    "tsx": "^4.19.2",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
